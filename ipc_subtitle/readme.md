# subttitle api

this example use a unique instance for renderize text on screen

- full screen transparent layout without user interaction
- api
  - for create and remove text on screen
  - for open and close window
  - render text in any position on window
